name: bloc_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  audioplayers: ^6.4.0
  searchable_dropdown_sheet: ^0.0.2
  modal_bottom_sheet: ^3.0.0
  permission_handler: ^12.0.1
  network_info_plus: ^6.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/sounds/click_sound.mp3
